#include<stdio.h>

int main(){
    // int arr[n],i=0,sum=0;
    // for(int i=0;i<n;i++){
    //     // printf("\narr[%d]=",i);
    //     // scanf("%d",&arr[i]);
    //     arr[i]=i;
    // }
    for(int i=0;i<4;i++){
        switch (i)
        {
        case 2:
            printf("%d",i);
        
        case 0:
            printf("%d",i);
        default:
            break;
        
        case 3:
            printf("%d",i);
        }
    }
    // int mean=(float)sum/n;
    // printf("\n Sum of elements is array = %d",sum);
    // printf("\n Mean of array = %d\n",mean);
}