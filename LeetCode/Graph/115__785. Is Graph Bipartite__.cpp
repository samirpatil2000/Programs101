#include<iostream>
#include<vector>
using namespace std;

class Solution {
public:
    bool isBipartite(vector<vector<int>>& graph) {
        vector<int> visited(-1,graph.size())

        bool bfs(vector<vector<int>>& graph,int src){
            
        }

        for(int i=0;i<graph.size(),i++){
            if visited[i]==-1{
                if bfs()
            }
        }
    }
};